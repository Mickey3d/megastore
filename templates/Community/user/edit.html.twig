{% extends 'base.html.twig' %}

{% block title %}{{ "Edit User"|trans }}{% endblock %}

{% block content %}
    {{ include('Community/user/_form.html.twig', {'button_label': 'Update'}) }}
    <a class="btn btn-primary btn-lg" href="{{ path('user_index') }}">{{ "Back to list"|trans }}</a>
{% if is_granted('ROLE_ADMIN') %}
    <div class="float-right">
      {{ include('Community/user/_delete_form.html.twig') }}
    </div>
{% endif %}
{% endblock %}
